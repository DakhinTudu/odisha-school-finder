spring.application.name=odisha-school-finder

# Database Configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/school_finder_db
#spring.datasource.username=root
#spring.datasource.password=password
#
## JPA Properties
#spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

server.port=${PORT:8080}

# DATABASE (will use JDBC_URL env var on Render)
spring.datasource.url=${JDBC_URL:}
spring.datasource.username=${DB_USER:}
spring.datasource.password=${DB_PASS:}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Ensure seed SQL runs (data.sql in resources)
spring.sql.init.mode=always

# CORS origin (set to Netlify later)
app.cors.allowedOrigin=${APP_CORS_ALLOWED_ORIGIN:*}
